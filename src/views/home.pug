extends base.pug
include mixins/video

block content
    h2 Welcome here you will see the trending Wetube videos
    p I Love #{lovesTarget}
    input(type="text" placeholder="What's your name")
    button send
    each item in videos 
        +video(item)
    else 
        div Sorry nothing found