mixin video(video)
    div
        h4
            a(href=`videos/${video.id}`)=video.title
        ul
            li Description : #{video.description}.
            li Posted : #{video.createAt}.
            li Hashtags : #{video.hashtags}.
            li #{video.views} views.
            li #{video.rating}/5.
            li ID : #{video.id}